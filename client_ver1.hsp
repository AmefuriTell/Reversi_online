#include "pcbnet2.as"

set_address

stop
#deffunc set_address
    address_txt = ""
    notesel address_txt
    noteload "address"
    noteget address, 0
    noteget port, 1
    port = int(port)
return
